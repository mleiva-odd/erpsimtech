/**
 * Fase 51 · Placeholder.
 *
 * Los tipos de Prisma son `export type` (no `interface`) y no permiten
 * declaration merging, así que para `User.lastLoginAt` usamos
 * `@ts-expect-error` en los puntos de uso. Una vez que se corra
 * `prisma generate` local, esos comentarios fallan (el tipo real incluye
 * el campo) y se pueden borrar.
 *
 * Este archivo existe para documentar el patrón. Borrable sin efecto.
 */
export {};
